# 项目协作规则

- 保持结构化输出，避免过分夸赞；优先准确性，对结论核验证据并明确未完成事项。
- 科研运行遇到旧目录无法清理、旧状态冲突等类似阻塞时，由监督者创建下一轮尚未使用的 `3CA/Q1_R<编号>` 和对应 supervisor 目录，不反复删除或复用旧研究目录。保留旧轮次及历史记录；不得绕过执行策略。
- 本次用户已删除旧 R12，允许重新建立空 R12 从零启动；以后同类阻塞按上述新目录规则处理。
- 新轮次须使用全新任务状态与正确轮次的提示、路径、启动和审计配置；只可复用核验来源与哈希后的公共原始缓存，不复制旧代码、计算或报告。
- 研究 agent 仅在本轮指定工作区内自主工作；下一轮目录的创建和切换由监督者负责。
- 每轮修改、实际测试结果、失败和启动事实均追加到 `C:/Users/User/Desktop/agentic/better.md`，保留既有记录。
