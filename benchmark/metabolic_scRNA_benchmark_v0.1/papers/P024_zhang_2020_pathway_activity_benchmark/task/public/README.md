# Agent-visible task package

Only this directory, the frozen shared gene set, and a materialized data snapshot may be mounted for a scored run. Do not mount `origin`, `curation`, or the separate private benchmark root.
