# R17 独立检查记录

检查时间：2026-09-13 20:31 +08:00。结论：核心结果与保存的数据相互一致；最终报告、验证及完成提交未通过，R17 不接受为完整成功轮次。

## 运行状态与检查范围

- 原 runner 已退出；未发现属于 R17 的研究 Node/Python/LaTeX 进程。最终复核发现 Windows 于 20:30:01 把 PID 15504 复用给了 `chrome.exe`，其命令行是 Chrome renderer。原审计脚本只检查 PID，后续因此误报 `runner_alive=true`；该布尔值不代表 R17 恢复运行。
- 持久检查点仍为 `running`，797 轮，最后更新于 20:27:02；没有 `runner-result.json` 或正常 `session_end`。现有日志不能确定退出原因，不能把这次退出称为正常完成或模型资源限制。
- 研究日志约 21.8 MB，记录两次原生核心调用、16 次验证工具调用、6 次完成提交请求。755 条有 function_call 事件的记录与 797 模型轮次不是同一计数。
- 工作流冻结差异为空。本次检查未修改研究代码、核心结果、TeX、manifest、PDF 或共享生产工具；只新增监督审计记录和四页 PDF 渲染预览。

## 已核实的核心证据

4 项核心输入哈希及 12 项核心输出 receipt 哈希均与当前文件一致；manifest 的核心哈希与第二次原生核心返回的哈希一致：`cf7f7045ca7faffdcf7af1ca7d81460da675731bf89c6218c28aa5b58f95b7bb`。当前引擎哈希与 receipt 一致：`ae6fc8ef52aa2d3061752f96cb0f016e1fd7efae01878c1a288ff2edc247d6fe`。

独立读取实际标签及原始细胞表，得到 58,843 条标签、58,843 个唯一 ID，标签 ID 集合与输入细胞 ID 集合完全相同；6 位患者、10 个样本、17 簇，最小簇 15 为 152 细胞，最大簇 10 为 9,017 细胞。匹配代谢基因为 1,984，选择分辨率为 0.4。本次没有从原始表达重新执行一遍独立实现，哈希与一致性检查不能证明全部分析方法均无误。

| 证据 | 实际结果 | 解释范围 |
| --- | --- | --- |
| 主代谢 Leiden silhouette | 0.216000393 | 主分区描述指标 |
| 代谢 matched-k KMeans silhouette | 0.239885524 | 与采用 KMeans 的基因集对照比较 |
| HVG KMeans silhouette | 0.287886649 | 高于代谢 matched-k KMeans |
| 19 个随机 KMeans silhouette | 0.201780647–0.235031128 | 相对代谢 matched-k KMeans 实际超过数为 0/19 |
| add-one rank fraction | 0.05 | (0+1)/(19+1)，非生物学 P 值 |
| 患者内 global-vs-within ARI | 0.147989255–0.795208764 | 描述性重聚类；最弱患者 2，914 细胞 |
| patient/sample/cell subtype NMI | 0.550964550 / 0.577583581 / 0.653843656 | 关联描述，不能证明独立 |

## 必须解决的问题

1. **P0：指定 PDF 未完成，编译输出路径不一致。** `report/main.pdf` 为 7,893 字节，没有 EOF，pypdf 报 `Stream has ended unexpectedly`。它和 `report/main.log` 停留在 18:01:20，log 有 `Missing $ inserted`。可读取的 PDF 在根目录 `main.pdf`，288,372 字节、4 页，20:13:49 生成；其 log 有 natbib 作者年份格式错误。当前 TeX 于 20:15:20 修改，晚于这两份 PDF。本次真实调用原验证 CLI 退出 1，仍报 PDF EOF 错误；没有通过。

2. **P0：对照比较混合分区方法，报告数值叙述矛盾。** 报告把 metabolic=0.216 与 HVG=0.288、随机最高=0.235 放在同一对照表，却声称没有随机对照超过代谢。实际有 13/19 个随机 KMeans 数值不低于 0.216；这不是同算法的公平比较，只用于指出表格文字的自相矛盾。原生核心的正确同算法基准是 metabolic matched-k KMeans=0.239885524，因而实际 0/19。正文未给出这个关键值，也未解释 add-one 0.05。

3. **P0：文献 DOI 误配。** manifest 的 reactome DOI `10.1093/nar/gkaa1081` 在本轮 Crossref 记录中对应端粒/复制研究，题名为 `Interstitial telomere sequences disrupt break-induced replication and drive formation of ectopic telomeres`。联网 [PubMed 原记录](https://pubmed.ncbi.nlm.nih.gov/33264397/) 再次确认此 DOI 属于该文。manifest 指定的 `sources/literature/crossref_reactome.json` 根本不存在；实际保存文件名为 `crossref_173ac8723004e5aa.json`。Reactome 正确来源须按 [官方引用说明](https://reactome.org/cite) 选取并重新核验，不能仅验证 DOI 能解析。原研究 bibliography 也没有保持 Crossref 作者顺序：第 7 位作者后直接追加末尾作者再写 et al.。

4. **P1：图注与图内容不符，图表公式引用合同未完成。** 实际 diagnostics 只有两个面板：分辨率/种子 silhouette 和 matched-k KMeans 基线；图注却描述三个面板并声称显示 pairwise ARI/NMI。UMAP 图有 cluster、cell type、patient 三面板，图注只解释 cluster。当前 TeX 有 2 个 figure、2 张三线表、1 个 equation，但正文 `ref/eqref` 数为 0，正文 cite 数为 0，公式没有 label，缺少来源和内容之间的引用关系。

5. **P1：manifest 缺少 feature_set.matched_genes_path。** 核心文件中实际存在该输出；科研合同要求在最终 manifest 中填写，而当前清单没有。EOF 异常导致验证器提前退出，未一次返回其余可修复问题。

6. **P1：方法与混杂报告不完整。** 报告未给出实际 QC 阈值及排除统计、软件版本、源基因/聚类/指标子抽样种子的区别、silhouette 子抽样与 CH/DBI 全细胞计算的区别。NMI 被主观称为 moderate，未呈现 cell subtype NMI=0.654、complexity/detected genes eta-squared 约 0.608；5 个簇（2、9、10、13、16）仅在 1 位患者中达到至少 5 细胞支持，正文未呈现。患者内重聚类不能作为留出患者验证，当前 summary 的离散生物学状态结论仍为 inconclusive。

7. **P0：最后提交摘要偏离真实核心。** 第 796 轮 complete_task 写出 5 簇、范围 1,234–18,567、选分辨率 0.5，并声称随机对照超过代谢。这些内容与保存的 17 簇、152–9,017、0.4 和 matched-k 0/19 冲突。完成 gate 拒绝了提交，没有真正完成任务。

## 工作流失效机制的实证

- 代理执行了 42 条包含自建/猜测 `validate_research_bundle.py` 的 shell 命令、18 条包含 pdflatex 的 shell 命令；重复读取和命令占据大部分后期轮次，持续运行本身没有修复输出路径或文献误配。
- 工作区的自建 validator 仅检查 PDF 大小、字段存在等，既不解析 PDF EOF，也不核对 bibliographic identity、核心 receipt 和最新源码的渲染；不具备原生科研验收的等价能力。
- 第 726 轮 `.\validate_research_bundle.py` 返回 CLIXML 和 `exit_code=0`，没有任何该脚本真正完成验证的输出，却被生成 verification_call_id，并反复用于提交。不能据此证明脚本已运行或验收通过。
- 第 697 轮 Python subprocess 打印 LaTeX `EXIT: 1`，外层 Python 仍退出 0，shell wrapper 生成了 verification_call_id。需要传递内部退出码并关联真实的编译/验收文件。
- 原生 complete_task 始终重新走科研 gate，EOF 错误阻止接受；最终 gate 生效，但错误呈现、文档编译入口及事实/文献匹配仍有待增强。
- 独立审计还需用可执行文件、命令行和进程创建时间核对 manifest 中的 PID；仅按 PID 是否存在会把 Windows 新建的无关进程误判为旧 runner。该错误已在本次 R17 的 Chrome PID 复用中实际出现。

## PDF 视觉检查

指定的 `report/main.pdf` 无法完整解析和渲染。为排查另一输出，监督者将根目录 `main.pdf` 四页全部渲染到 `supervisor/Q1_R17/pdf-review` 并逐页查看。这是一份早于最新 TeX 的排查材料，不能作为合格最终报告。页面文字和三线表基本可读，无明显裁切；第 3 页 Figure 1 浮动到小节标题之前，两条 figure 小节标题挤在两张图之间，图注和实际面板不符，正文也未引用这些图。最新源码尚无对应的完整 PDF，因此最新排版验收仍未完成。

## 后续修复顺序

优先提供固定 report 输出目录、正确参数、内部退出码传播和双次编译的原生报告构建入口；随后让 bundle 验证捕获损坏 PDF 并一次返回结构化问题；加强正文/BibTeX 与 Crossref 题名、作者、卷页的一致性，以及 matched-k 数值和最终摘要事实的一致性。修复后按项目规则启动下一新轮次，保留 R17 现场。本次检查没有启动 R18 或续跑 R17。
