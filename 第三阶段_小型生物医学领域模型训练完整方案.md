# 第三阶段：小型生物医学领域 Agent 模型训练完整方案

> 版本：v1.0（研究设计稿）  
> 检索与核对截止：2026-09-11  
> 上游依赖：第一阶段 benchmark、第二阶段可靠 agentic workflow、会议记录和芯片领域材料  
> 核心范围：从经过验证的生物医学工作流运行记录中构造训练数据，训练 8B 级模型；根据证据决定是否扩展到约 16B/32B 和在线强化学习  
> 非目标：训练一个脱离外部数据、工具、统计软件和人类审核的“全知生物学家”

---

## 0. 执行摘要

第三阶段最值得训练的对象不是“论文知识的压缩包”，也不是模型不可核验的私有思维链，而是一个**在冻结工作流中作出正确下一步决策的策略模型**。它应学会：

1. 读取任务合同、数据状态和前序工件；
2. 选择正确 skill 或工具并生成合法参数；
3. 根据真实执行结果继续、修复、回退或停止；
4. 遵守统计推断单位、数据版本、负对照和敏感性分析等科学不变量；
5. 把每条结论绑定到可复核的计算工件或文献证据；
6. 在证据不足、设计不可识别或数据不支持时给出 `inconclusive`，而不是强行复现论文结论。

本项目的推荐路线是：

```text
Phase 1 冻结 benchmark 与泄漏组
              ↓
Phase 2 运行统一 workflow，保存可观察轨迹与验证结果
              ↓
轨迹数据工厂：许可 → 脱敏 → 校验 → 分段 → 去重 → 分割 → 派生训练视图
              ↓
4B 仅做数据/训练管线烟雾测试
              ↓
一个 7–9B instruction 模型做 QLoRA/LoRA SFT
              ↓
同一 benchmark 上比较 Base / Base+Flow / SFT / SFT+Flow
              ↓
若 SFT 有稳定增益：用验证器和专家偏好对做 DPO
              ↓
只有可执行奖励与专家评价一致且通过反作弊测试时，才做多轮 tool-in-the-loop RL
              ↓
按残余错误类型决定是否比较约 16B；32B 是条件性上限，不是默认下一步
```

最重要的三个决策是：

- **先训练 8B，不并行启动 8B、16B、32B 三条线。** 模型规模不是第一个变量；先证明轨迹训练在相同 workflow 下确实增加科学正确性。
- **先 SFT，再偏好优化，最后才是 RL。** MINT 的实验提示，常规 instruction fine-tuning 或 RLHF 并不自动改善多轮工具交互；训练是否有用必须由本项目的受控实验回答，而不能预设。[^mint]
- **保留 skills、工具、状态机和验证器。** 训练的目标不是把第二阶段的可审计结构“蒸馏掉”，而是降低其中 LLM 决策部分的成本、延迟和外部依赖。

一个务实的 v0.1 应只有：一个 8B student、一个冻结 workflow、八个核心 skills、SFT 和一套封闭评价。DPO、RL、16B/32B、continued pretraining 和独立 verifier 小模型都应由错误分析触发，而不是一开始全部建设。

---

## 1. 证据边界：哪些已有经验可以继承，哪些仍是研究假设

### 1.1 芯片设计材料能直接支持什么

| 工作 | 已公开做到的事情 | 对本项目的直接启示 | 不能据此宣称的事情 |
|---|---|---|---|
| RF-Agent | 从 7 本 RF 教材的 1,108 个小节构造超过 11,000 条 QTSA 样本，用 SFT 提升小中型模型的 RF 推理[^rfagent] | 原始专业材料可以经过结构化、质量控制后转成小模型可学的领域样本；题目、思考摘要、解法和答案可分字段保存 | 它不是从真实 EDA 闭环执行轨迹训练 8B agent；教材问答增益不能等同于工具工作流增益 |
| PDAgent-Bench | 353 个任务覆盖知识、报告理解、根因分析、脚本和完整物理设计流程，并通过工具闭环评价[^pdagent] | benchmark 必须同时保留原子能力题和长程 workflow；真实工件、可执行解和细粒度 rubric 都有价值 | 论文构建并评价 workflow，但没有公开证明“用这些完整轨迹微调 8B” |
| HeaRT | 把电路表示为层级推理树，再按问题检索相关分支并交给确定性优化器[^heart] | 结构、状态和领域知识应外置；小模型不应在每一步重新消化整个历史 | HeaRT 不是 student 轨迹蒸馏论文；其电路指标不能映射为生物医学预期增益 |
| AnalogSeeker | 多 agent 从教材蒸馏细粒度问题、答案和推理，微调 32B 模型并报告领域问答与运放设计增益[^analogseeker] | 领域知识蒸馏可以作为辅助数据层；限制参数漂移值得做消融 | 它主要验证知识与设计辅助，不是完整工具执行轨迹训练 |
| VeriReason | 由 specification、reasoning、RTL、testbench 构成数据；SFT 后用语法、结构和 testbench 结果做 GRPO[^verireason] | “执行器是真值反馈”以及“先冷启动 SFT、后执行反馈 RL”可迁移；困难度应按当前模型 rollout 的成功分布选择 | Verilog 有近乎确定的功能测试；组学发现通常没有同等完备的 testbench，不能照搬奖励 |
| 附件中讨论的遗忘/后训练与 workshop 工作 | 提供模型遗忘、可靠性和多轮执行反馈方向 | 提醒我们保存数据谱系、保留可删除 adapter、重视多轮环境 | 不构成真实 EDA 轨迹训练 7B/8B 已经成熟的证据 |

因此，芯片领域给出的最可靠结论是：**知识蒸馏、可执行验证、层级状态和闭环反馈各自有效；把四者整合为真实科学发现轨迹并训练小模型，仍然是需要本项目验证的研究空白。**

### 1.2 生物医学中最接近本项目的公开证据

截至检索日，ATHENA-R1 是最接近“生物医学工具轨迹 → SFT → 多轮执行反馈 RL”的公开例子。其方法报告：

- 从 85,340 条推理轨迹生成 378,027 个 instruction-tuning 样本；
- 轨迹含 177,626 个推理步骤和 281,695 个工具调用；
- 把每个步骤拆成“系统与问题、前序轨迹、可用工具 → 当前思考、工具调用”，只对输出 token 计算 SFT loss；
- 对工具名称、描述和参数名称做改写，加入干扰工具并打乱工具顺序；
- 用 LoRA 训练 8B 模型；其公开配置中，8B SFT 使用 4 张 H100（总显存 320 GB），约 9.93 GPU-days；
- 随后在 212 个生物医学工具环境中做多轮 GRPO，并用答案、格式、工具参数来源、证据收集和冗余等规则反馈训练。[^athena-methods]

这证明了路线的**技术可行性**，但不能直接证明其适合本项目，原因包括：

1. ATHENA-R1 主要是药物与治疗推理，本项目主要是原始组学数据分析、科学再发现和开放假设；
2. ATHENA 的训练题具有可映射的正确选项，本项目不少任务的正确输出是效应量、置信区间、排序、工件集合或“不可判定”；
3. ATHENA 奖励“至少两个工具步骤”和限定回答长度；这在我们的任务上可能鼓励不必要调用，不能直接采用；
4. 本项目必须防止 student 只学会重述目标论文的结论，而没有从输入数据重新得到结果。

另外几条关键生物医学证据是：

- CRISPR-GPT 用约 4,000 个公开讨论线程整理出 3,000 多个问答对，微调 Llama3 8B；论文报告相对基座在基础问题和真实研究问题上分别有约 8% 和约 20% 的提升。它说明小规模、高质量领域讨论可以改善 8B，但该模型在系统中主要是 brainstorming source，不是完整 agent policy。[^crisprgpt]
- BioDSA-1K 从 329 篇论文构造 1,029 个假设任务和 1,177 个分析计划，并明确包含“现有数据不足以验证”的任务；这支持把正确拒绝和不可验证结论作为训练与评价的一等类型。[^biodsa]
- 最新的 BixBench-Verified-50 预印本显示，skills、当前工具和清理后的题目可大幅改变表现；但它只有 50 题、每配置单次运行且是预印本，所以它更支持“训练前先优化环境和题目”，而不是支持某个训练收益数字。[^skill-bix]
- 生物医学领域微调并不总是改善真实任务；一项跨模型评估发现，多种 biomedical-tuned 模型在幻觉、编码和指令遵循任务上反而退步。[^biomed-ft]

### 1.3 第三阶段要验证的研究假设

以下不能写成先验结论，必须由消融实验验证：

- H1：完整的可观察工作流轨迹优于只训练最终问答；
- H2：包含 validator 反馈的“失败 → 修复”样本优于只保留一次成功样本；
- H3：SFT student 与外部 skills 存在互补，而不是 student 只在无 workflow 时有效；
- H4：8B + workflow 可以在部分任务上接近或超过 frontier model + direct prompt；
- H5：DPO 能降低伪重复、过度因果、证据缺失和不当重试，而不损害正确发现能力；
- H6：在具有可靠可执行奖励的任务上，多轮 RL 能进一步提高科学工件正确性；
- H7：16B/32B 的收益主要出现在真正的长程规划和跨证据整合，而不是 schema 与工具格式错误。

---

## 2. 第三阶段的产品定义

### 2.1 建议训练的模型

名称暂定为 `BioDiscovery-Workflow-Student`。它是第二阶段状态机中的**策略模型**，输入为当前可见状态，输出为一个受约束的动作：

```text
πθ(action, decision_summary | task_contract,
                              current_state,
                              artifact_summaries,
                              validator_feedback,
                              available_skills,
                              available_tools)
```

它负责：

- 任务澄清与可答性判断；
- 当前状态下的 skill/tool 选择；
- 工具参数和分析代码生成；
- 读取真实输出并选择下一步；
- 错误诊断、回退和有上限的重试；
- claim/evidence 绑定；
- 不确定性语言和停止决策。

它不负责：

- 在参数中记住数据库的最新内容；
- 用语言模型心算替代 R/Python/statistical software；
- 修改冻结数据或 grader；
- 绕过数据许可、患者权限或网络策略；
- 单独担任独立 reviewer；
- 把表达富集解释为代谢通量或因果机制；
- 在临床患者层面自动给出诊断或治疗建议。

### 2.2 最小运行架构

v0.1 不需要训练多个专用模型。最小架构是：

```text
一个 8B student（Planner + Analyst 的受控决策）
            │
            ├── 第二阶段冻结的 skills
            ├── Python/R/CLI 沙箱和数据/文献工具
            ├── V1–V7 确定性 validators
            ├── 外部状态与工件库
            └── 独立 reviewer（前沿模型或人类，初期不训练）
```

只有在错误分析证明“生成者与审查者需要不同能力”，并且有独立 reviewer 标注集时，才考虑单独训练 verifier。用同一批轨迹同时训练 generator 和 verifier，再让二者互相证明，会制造相关错误，不构成独立验证。

### 2.3 训练后仍必须保留 workflow

模型训练后不能绕过以下硬门：

- 数据 accession、版本和 SHA-256；
- cell → sample → patient → study 层级；
- counts/normalized/integrated 层状态；
- 设计矩阵可识别性；
- 推断单位；
- gene ID/物种/背景 universe；
- 主分析、敏感性和负对照；
- 可重现入口脚本；
- claim → artifact/evidence 链；
- 安全、许可和人工 gate。

第三阶段的价值是让模型**更稳定地使用这些结构**，而不是把结构替换成模型记忆。

---

## 3. 开始训练前的 Go/No-Go 门槛

### 3.1 必须全部满足的准备条件

| Gate | 检查内容 | 通过标准 | 不通过时的动作 |
|---|---|---|---|
| G0 Benchmark freeze | 任务、输入、grader、leakage group、sealed test | 有版本号、manifest 和不可由训练进程访问的 test gold | 先完成第一阶段，不训练 |
| G1 Workflow stability | Phase 2 schema 与八个核心 skill | 连续两个小版本无破坏性 schema 变化；旧 RunBundle 可迁移或重放 | 先稳定 workflow |
| G2 Workflow value | `Base+Flow` 相对 `Base+Direct` | 在多个任务族中有方向一致的收益，bootstrap/层级模型 CI 支持非偶然；hard-failure 不增加 | 若无收益，修 workflow 而非微调 |
| G3 Trace volume | 经验证的完整 episode | 建议至少 2,000 个可训练 episode；同时覆盖成功、修复、正确停止和 inconclusive | 不足时继续采集，而非大量复制同一题 |
| G4 Trace diversity | 论文、队列、癌种、skill、错误类型 | 不由少数论文族或单个教师占主导；关键 skill 都有正负示例 | 定向补数 |
| G5 Verifiability | 执行、validator、artifact、claim | 所有正例可从冻结环境重放；失败有机器或专家可解释标签 | 不可重放样本隔离 |
| G6 Rights/privacy | 数据、论文、教师输出、模型许可 | 每个 episode 有训练用途字段；受控数据有 DUA/伦理/合作授权 | 未授权样本不得进入训练 |
| G7 Baseline | 未训练模型的 P0/P1 结果 | 有固定模型、解码、预算、三次以上重复或适当任务级 bootstrap | 先建基线 |

这里的“2,000 个 episode”是项目规划下限，不是文献证明的普适样本复杂度。真正的数据量应由 learning curve 决定：分别用 10%、25%、50%、100% 数据训练，观察新论文族上的边际收益，而不是先认定“越多越好”。

### 3.2 不应开始训练的典型情况

- Phase 2 还在频繁改变 tool-call schema；
- 主要失败来自下载链接失效、依赖未安装或 validator 错误；
- 只有三篇种子论文的重复运行；
- student 的 sealed test 论文已经用于教师提示、RAG 或人工示范；
- 数据只保留最终报告，没有工具输入、真实输出和工件；
- 绝大多数轨迹都由同一个教师在看见 gold 后生成且未经无 gold 重放；
- 想通过训练让模型记住最新数据库，而不是运行时检索；
- 奖励只看“是否与论文结论一致”或“p < 0.05”。

---

## 4. 什么是本项目的“可训练轨迹”

### 4.1 不保存或不依赖私有 Chain-of-Thought

“思维链”需要拆成三个不同概念：

| 内容 | 是否保存 | 是否可训练 | 原因 |
|---|---:|---:|---|
| 模型提供商内部、不可见的 hidden CoT | 否 | 否 | 不可获得、不可审计，且可能受平台和使用条款约束 |
| 模型生成的长篇自由推理文本 | 原始日志可按许可隔离；默认不作为核心监督 | 谨慎 | 可能是事后合理化，不保证忠实；也会鼓励冗长 |
| 可观察决策摘要 `decision_summary` | 是 | 是 | 说明当前证据、选择动作的领域理由、停止条件和不确定性；可由事件和工件核验 |
| 工具调用、参数、代码、工具输出、validator、修复动作 | 是 | 是 | 这是环境中真实发生的、可执行的因果链 |
| claim/evidence/provenance | 是 | 是 | 可检查最终语言是否受证据支持 |

已有研究表明，模型生成的 CoT 可能系统性遗漏真正影响答案的线索，并为错误答案构造看似合理的解释；因此不能把“长而流畅的 reasoning”当作轨迹真实或安全的证明。[^cot-faithfulness] 本项目把**事件日志和可执行工件作为事实源**，自然语言理由只是其受约束的摘要。

推荐的 `decision_summary` 模板：

```yaml
observed:
  - artifact://sha256/... 显示 patient_id 缺失
decision: stop_and_request_metadata
because:
  - 当前问题要求患者级差异，细胞不能作为独立患者重复
  - 缺少 patient/sample 映射时设计矩阵不可构造
alternatives_rejected:
  - cell_level_wilcoxon: pseudoreplication_risk
resume_when:
  - 提供 cell_id → sample_id → patient_id 映射
confidence: high
```

这些字段可以逐条核验；它不要求也不声称暴露模型内部神经计算过程。

### 4.2 一个完整 episode 的内容

```text
任务合同 task.yaml
  ↓
数据与环境 manifest
  ↓
S0...S13 每个状态的 observation
  ↓
模型 action / tool call / code
  ↓
真实 tool output 与 artifact refs
  ↓
validator 结果
  ↓
下一动作、回退、修复或停止
  ↓
claims/evidence/hypotheses/provenance
  ↓
最终 task-specific score、成本、失败类型、人类审核
```

每个 episode 至少需要：

- `episode_id`、`task_id`、`benchmark_version`；
- `paper_family_id`、`dataset_family_id`、`leakage_group_id`；
- 输入数据和环境哈希；
- 模型、system prompt、skill/tool 版本；
- 每一步的状态、可见 observation、动作、执行结果和 validator；
- 所有大工件的内容寻址引用；
- 最终科学分数与 hard-gate 状态；
- 教师是否看过 gold；
- 数据/教师输出/模型权重的许可与发布等级；
- 去标识化与安全扫描结果。

### 4.3 六类轨迹

| 类型 | 输入给 student 的前缀 | 训练目标 | 主要用途 |
|---|---|---|---|
| 一次成功 | 正确状态与前序输出 | 下一正确动作及简短理由 | SFT 基本策略 |
| 失败后修复 | 错误工具输出或 validator 反馈 | 诊断、修复参数、重跑或回退 | SFT 学恢复能力 |
| 正确停止 | 证据不足、完全混杂、权限不足 | `stop/inconclusive/request_input` | 校准和安全 |
| 反事实/对抗 | 错 accession、错误 gene ID、伪重复诱导、gold 诱导 | 拒绝捷径并指出冲突 | 偏好优化和鲁棒性 |
| 好/坏成对 | 完全相同状态前缀 | chosen/rejected 两个动作 | DPO/排序器 |
| 多轮探索 | 无 gold 的任务和真实工具反馈 | 整条可执行策略及最终工件 | full-episode SFT 与后续 RL |

失败动作本身不能作为 SFT 的目标。正确做法是把失败作为 `tool`/`validator` observation，监督模型输出下一条修复动作；原错误动作进入 preference 的 `rejected` 字段或错误分析库。

---

## 5. 训练数据从 Phase 2 如何生成

### 5.1 原始事实源与派生视图

第二阶段 RunBundle 继续作为唯一事实源：

```text
run.json
events.jsonl
state.json
plan.json
artifacts/
claims.jsonl
evidence.jsonl
hypotheses.jsonl
provenance.json
validator_reports/
```

第三阶段**不修改这些原始记录**，只建立可重建的训练派生视图。这样未来换模型 chat template、截断策略或训练框架时，不需要重新执行全部科学分析。

### 5.2 轨迹数据工厂

```text
1. Eligibility
   检查任务分割、许可、隐私、环境与完成状态
2. Canonicalization
   统一状态名、角色、工具 schema、ID、时间、错误码
3. Artifact binding
   大对象留在 artifact store；事件仅保存哈希、类型和摘要
4. Verification
   重放入口脚本、运行 V1–V7、比较容差和 claims
5. De-identification
   移除密钥、用户路径、患者/人员标识、内部 URL 和未授权字段
6. Rationale grounding
   从可观察事件生成或人工编写 decision_summary，并核验引用
7. Segmentation
   生成 state-local 样本、恢复样本和完整 episode
8. Deduplication
   按论文族、数据集、任务模板、代码 AST/文本近似和输出指纹去重
9. Split lock
   按 leakage group 绑定 train/dev/test；禁止逐 turn 随机切分
10. Render
   生成 SFT、preference、RL prompt 等 JSONL/Parquet 视图
11. Audit & hash
   生成 dataset manifest、质量报告、版权/隐私报告和 SHA-256
```

最少实现为一个确定性构建程序即可，不需要先搭数据库服务。原始事件使用 JSONL，大规模索引与统计使用 Parquet，配置和人读规则使用 YAML/Markdown。

### 5.3 推荐目录结构

```text
training/
├── schemas/
│   ├── episode.schema.json
│   ├── sft_step.schema.json
│   ├── preference.schema.json
│   └── reward.schema.json
├── releases/
│   └── v0.1.0/
│       ├── dataset_manifest.json
│       ├── dataset_card.md
│       ├── split_assignments.parquet
│       ├── episode_index.parquet
│       ├── views/
│       │   ├── sft_step.train.jsonl
│       │   ├── sft_episode.train.jsonl
│       │   ├── preference.train.jsonl
│       │   └── rl_prompts.train.jsonl
│       ├── audits/
│       │   ├── quality_report.json
│       │   ├── leakage_report.json
│       │   ├── privacy_report.json
│       │   └── license_report.json
│       └── hashes.sha256
├── configs/
│   ├── sft_8b.yaml
│   ├── dpo_8b.yaml
│   └── rl_8b.yaml
├── runs/
│   └── <training_run_id>/
│       ├── training_run.yaml
│       ├── metrics.jsonl
│       ├── checkpoints.json
│       └── eval_results/
└── model_cards/
    └── <checkpoint_id>.md
```

`artifacts/` 不复制进每个 release；训练记录只引用 Phase 2 内容寻址对象，例如 `artifact://sha256/<hash>`。发布时根据许可生成公开、受限和不发布三个版本。

### 5.4 `episode_index.parquet` 的必要列

| 字段 | 示例 | 用途 |
|---|---|---|
| `episode_id` | `ep_WF3CA_MP_000184` | 全局关联 |
| `task_id` | `WF-3CA-MP-01-T017` | 对应 benchmark |
| `task_family` | `program_discovery` | 平衡与分层评价 |
| `paper_family_id` | `pmid_374..._family` | 泄漏控制 |
| `dataset_family_id` | `3ca_2023_release` | 队列隔离 |
| `leakage_group_id` | `lg_01J...` | 原子分割单元 |
| `split` | `train` | 不可由训练脚本修改 |
| `outcome` | `success/repaired/inconclusive/fail` | 样本类型 |
| `gold_access` | `none/helper/reviewer` | 识别 gold-assisted 轨迹 |
| `hard_gate_pass` | `true` | SFT 正例资格 |
| `replay_pass` | `true` | 可重现性 |
| `expert_review` | `accepted/conditional/rejected` | 人审状态 |
| `license_tier` | `public_train/restricted_train/no_train` | 使用范围 |
| `privacy_tier` | `public/controlled/patient` | 隔离策略 |
| `teacher_model` | 精确模型快照 | 教师溯源 |
| `run_bundle_uri` | 内容寻址 URI | 回到原始证据 |

### 5.5 SFT 的 step-level JSONL

一个训练样本应让模型只预测当前正确动作，而不是复述所有工具输出：

```json
{
  "sample_id": "sft_ep184_s04_03",
  "episode_id": "ep_WF3CA_MP_000184",
  "leakage_group_id": "lg_01J...",
  "state": "S4_PLAN_REVIEW",
  "messages": [
    {
      "role": "system",
      "content": "You are the workflow policy. Use only visible artifacts and registered skills."
    },
    {
      "role": "user",
      "content": "<task_contract_ref>...</task_contract_ref><state>...</state><observations>...</observations>"
    },
    {
      "role": "tool",
      "name": "data_contract_auditor",
      "content": "{\"counts_layer\":\"raw\",\"patient_id_present\":true,\"n_patients\":58}"
    },
    {
      "role": "assistant",
      "content": "<decision_summary>患者是目标推断单位；当前具备患者映射，应先生成 sample-aware 设计矩阵并做可识别性检查。</decision_summary>",
      "tool_calls": [
        {
          "id": "call_003",
          "type": "function",
          "function": {
            "name": "sample_aware_statistics",
            "arguments": "{\"inference_unit\":\"patient\",\"formula\":\"program_score ~ cancer_type + batch\",\"preflight_only\":true}"
          }
        }
      ]
    }
  ],
  "target_message_index": 3,
  "artifact_refs": ["artifact://sha256/abc..."],
  "validator_labels": {"schema": 1, "inference_unit": 1, "no_gold_leak": 1},
  "provenance": {"teacher": "...", "gold_access": "none"},
  "license_tier": "public_train"
}
```

训练时只对目标 assistant 输出计算 loss；system、user、tool observation 及前序历史全部 mask。ATHENA-R1 也采用只监督输出 token 的做法，避免模型学习复述工具结果。[^athena-methods]

### 5.6 Full-episode SFT JSONL

原子样本让模型学习单步正确，但会弱化长程状态一致性。需要保留一部分完整 episode：

```json
{
  "episode_id": "ep_WF3CA_MET_000071",
  "messages": [
    {"role": "system", "content": "..."},
    {"role": "user", "content": "..."},
    {"role": "assistant", "tool_calls": ["..."]},
    {"role": "tool", "content": "..."},
    {"role": "assistant", "content": "...", "tool_calls": ["..."]},
    {"role": "tool", "content": "..."},
    {"role": "assistant", "content": "<final>...</final>"}
  ],
  "loss_mask": "assistant_actions_and_outputs_only",
  "outcome": "repaired_success",
  "score_ref": "artifact://sha256/score..."
}
```

对超过训练上下文的 episode，不应粗暴截掉最近状态。使用第二阶段的状态摘要和 artifact refs；保留：任务合同、当前状态、冻结计划、关键 validator、最近若干步、claim ledger 摘要。完整工具输出仍可按哈希读取。

### 5.7 Preference JSONL

```json
{
  "pair_id": "pref_ep184_s04_11",
  "leakage_group_id": "lg_01J...",
  "prompt_messages": ["...完全相同的状态前缀..."],
  "chosen": {
    "decision_summary": "按 patient 聚合后进行 pseudobulk 主分析，并把 mixed model 作为敏感性分析。",
    "tool_calls": ["..."]
  },
  "rejected": {
    "decision_summary": "细胞数很大，可直接在 cell level 检验获得更高统计功效。",
    "tool_calls": ["..."]
  },
  "preference_basis": [
    "validator:inference_unit",
    "expert:biostatistics_review"
  ],
  "severity": "critical",
  "license_tier": "public_train"
}
```

偏好必须在**相同前缀**下比较，避免 chosen 只是因为看到了更多信息。每个 pair 记录机器规则、专家判断和严重程度；若二者冲突，先进入 adjudication，不直接训练。

### 5.8 RL prompt 与 rollout

RL 数据不预先存一条“正确轨迹”，而是存环境入口：

```json
{
  "rl_prompt_id": "rl_WF3CA_MP_T017",
  "task_id": "WF-3CA-MP-01-T017",
  "leakage_group_id": "lg_train_017",
  "environment_image": "sha256:...",
  "initial_state_ref": "artifact://sha256/...",
  "allowed_tools_ref": "toolset://v0.1.0/mp",
  "skillset_ref": "skillset://v0.1.0/core8",
  "budget": {"turns": 24, "gpu_minutes": 120, "external_calls": 8},
  "grader_ref": "grader://WF-3CA-MP-01/v1",
  "gold_visibility": "grader_only"
}
```

每次 rollout 另存 `rollout_id`、全部 events、分项 reward、终止原因、资源成本和模型版本。训练模型只能看到环境反馈，不能读取 grader 的隐藏 gold。

---

## 6. 数据来源与优先级

### 6.1 五层数据

| 层 | 来源 | 可信度 | 用途 | 限制 |
|---|---|---:|---|---|
| L0 | 领域专家在训练 split 上完成的金标准 workflow | 最高 | SFT 种子、rubric 和错误本体 | 成本高；仍需重放与双审 |
| L1 | 前沿模型在 Phase 2 workflow 中生成并通过执行/validator 的轨迹 | 高 | 主体 SFT 与恢复数据 | 教师偏差、许可和 gold 暴露必须记录 |
| L2 | 多教师产生的候选动作，经确定性执行和专家裁决选择 | 中高 | 增加策略多样性和 preference pair | 不能用多数票替代科学验证 |
| L3 | 从训练论文的方法、数据 schema 和反事实变换生成的合成任务 | 中 | 长尾错误、负对照、新工具泛化 | 必须可执行验证；不得污染 test 论文 |
| L4 | 真实项目/BKI 工作流日志 | 条件性 | 分布内适配、私有 adapter | 默认不得训练；需 DUA/伦理/合作书面授权和脱敏 |

### 6.2 教师轨迹生成规则

每个训练任务推荐产生多条候选，而不是只保存第一次输出：

1. 在完全相同的 Phase 2 环境中运行 2–3 个具有不同能力侧重的教师；
2. 对每个教师使用固定温度/预算并保留精确模型快照；
3. 运行所有确定性工具和 validators；
4. 按科学工件分数、hard gate、成本和专家判断排序；
5. 选择一个或多个不同但等价的有效策略作为正例；
6. 把常见错误保存为 `rejected`，而不是删除得无影无踪；
7. 对选择的轨迹在无 gold 条件下重放，确认不是靠答案提示完成。

ATHENA-R1 的 TraceGen 使用能看见答案的 Helper 引导 Solver，这对大规模构造正确轨迹有效，但本项目必须额外防止“把 gold 写进路径”。[^athena-methods] 因此设两种标签：

- `gold_access=helper`：只允许在 train split 构造候选；必须做无 gold replay；
- `gold_access=none`：来自真实盲法运行，优先用于 full-episode SFT 和泛化分析。

任何 `test/private gold` 暴露都使该 episode 永久进入 quarantine，不能通过“删掉一句答案”重新获得训练资格。

### 6.3 合成数据的允许与禁止

允许：

- 更换基因别名、物种 ID、列名和文件顺序，测试 schema 鲁棒性；
- 注入缺失 patient ID、混杂设计、错误数据层、错误背景 universe；
- 构造效果为零、方向相反、亚组不一致和负对照失败的数据；
- 对工具描述改写、加入相似干扰工具、打乱工具顺序；ToolACE 和 ATHENA 都显示这种多样化有助于模型依赖语义而非位置/名称记忆。[^toolace][^athena-methods]
- 从真实错误日志中生成最小复现和修复对。

禁止：

- 仅由 LLM 自评“看起来正确”就进入正例；
- 把目标论文摘要改写成问题和答案，却不给 student 原始数据；
- 为了增加条数而对同一轨迹做大量同义改写；
- 合成只包含正结果的数据；
- 使用 sealed test 的标题、作者、数据 accession、图注或结论生成训练任务；
- 未核对服务条款就把闭源教师输出用于训练另一个模型。

---

## 7. 数据资格、质量分层与平衡

### 7.1 SFT 正例的硬门

一个 episode 只有同时满足以下条件才可产生正例：

```text
eligible = split_is_train
        AND training_rights_ok
        AND privacy_check_pass
        AND no_gold_leak
        AND data_identity_pass
        AND environment_pinned
        AND replay_pass
        AND all_critical_validators_pass
        AND claims_resolve_to_artifacts
        AND expert_status != rejected
```

以下情况即使最终答案与论文一致也不能作为正例：

- 错 accession 或错误数据版本；
- cell-level pseudoreplication 得到“正确方向”；
- 使用 integrated matrix 做不适当 DE；
- gene set 物种/ID 映射错误；
- 多次调参后只选择显著结果；
- 图表数字无法从输出表重建；
- claim 引用不存在的工件；
- 把相关、富集或表达写成因果、代谢通量或临床疗效；
- 读取了 gold notebook、答案或隐藏 grader。

### 7.2 质量分层

硬门之后再分层，而不是把所有指标加权成一个分数来掩盖致命错误：

- `Tier A`：可重放、所有 hard gate 通过、专家接受、无 gold；
- `Tier B`：可重放、所有 hard gate 通过、自动验证充分、专家抽查接受；
- `Tier C`：gold-assisted 后无 gold 重放成功，或存在非关键 reviewer 条件；
- `Tier D`：可作为 rejected/诊断样本，但不能作 SFT 正例；
- `Quarantine`：泄漏、许可、隐私或不可重放。

首轮 SFT 以 A/B 为主，C 控制比例；D 只用于偏好与错误识别。

### 7.3 推荐的规模阶梯

这些是**工程起始目标**，不是保证性能的经验定律：

| 阶段 | 完整 episode | 派生 state/action 样本 | 论文/数据族 | 目的 |
|---|---:|---:|---:|---|
| Pipeline smoke | 200–500 | 5,000–15,000 | 10–20 | 检查 schema、loss mask、工具格式、训练是否收敛；可用 4B |
| v0.1 8B SFT | ≥2,000 | 20,000–60,000 | 30–60 | 首个严格 Base/SFT 因果比较 |
| v0.5 | 5,000–10,000 | 60,000–150,000 | 60–120 | 增加恢复、inconclusive、跨癌种和新工具 |
| v1.0 | 10,000–20,000 | 100,000–300,000 | 100–200 | 支持 8B 稳定模型和约 16B/32B learning curve |

RF-Agent 的 11,000+ QTSA、FireAct 的 500 条教师轨迹和 ATHENA-R1 的 85,340 条轨迹说明不同任务的数据需求可跨数个数量级；不能把任何单一数字直接迁移。FireAct 报告 500 条 GPT-4 agent 轨迹即可显著改善 Llama2-7B 的 HotpotQA agent 表现，但那是搜索问答，不代表复杂组学 workflow 也只需 500 条。[^fireact]

### 7.4 平衡轴

每次 release 必须报告而不是隐藏以下分布：

- task family：数据定位、QC、DE、program、pathway/metabolism、TCR、文献、claim；
- workflow state：S0–S13；
- outcome：一次成功、修复成功、正确停止、inconclusive、失败；
- difficulty：原子动作、短链、长链、跨工具、跨证据；
- modality：bulk、single-cell、TCR、多组学、表格、文本；
- scientific result：正、负、异质、不确定；
- failure family：版本、schema、推断单位、统计、代码、检索、解释、证据、安全；
- teacher/model：防止一种语言风格垄断；
- paper/dataset family：防止大数据集产生的大量 turns 淹没小数据集；
- language：英文工具与论文、中英文用户任务。

采样单位应是 `leakage_group/episode`，不是 turn。否则一个 200 步长 episode 会压过几十个短任务，student 只学会它的局部模式。

---

## 8. 数据分割与污染防护

### 8.1 分割必须早于轨迹生成

第一阶段一旦确定 `leakage_group_id`，第三阶段直接继承，不能为了增加训练数据重新随机分割。一个 leakage group 至少包含：

- 同一主论文及补充材料；
- 预印本、正式版和更正；
- 同一数据 accession 的所有衍生任务；
- 作者仓库、notebook、reference code 和镜像；
- 从同一 figure/table/claim 派生的问题；
- 外部验证中与主任务共享样本的队列；
- 使用相同随机模拟种子或模板生成的近重复任务。

### 8.2 三层测试

| 测试层 | 用途 | student/教师可见性 |
|---|---|---|
| Dev | 选 checkpoint、阈值和超参数 | 输入可见，gold 只给 evaluator；不得训练 |
| Sealed test | 第一阶段正式 leaderboard | 训练与教师生成完全不可见；最终少次数运行 |
| Rolling temporal/OOD | 新论文、新数据版本、新癌种、新工具 | 在模型冻结后加入；检验真实泛化 |

为了抵消基础模型可能已见过公开论文，应增加：

- 发表/公开时间晚于基座知识截止的任务；
- 仅提供数据和匿名科学问题、不提供论文标题的重发现任务；
- 内部但无患者标识、明确授权的隐藏合成或实验任务；
- conclusion-flipped/null 数据；
- 同一方法在新队列上的 external validation。

### 8.3 污染扫描

release 前检查：

- train 文本是否出现 test DOI、PMID、accession、标题、作者组合、figure caption；
- 代码或注释是否包含 reference answer；
- RAG index 是否索引 sealed 论文；
- skill 文档是否举了 test 任务的具体答案；
- 教师 prompt 是否包含 test rubric；
- 近似代码、表格和数值指纹是否跨 split；
- 模型评测时是否可通过网络搜索直接定位目标论文。

污染报告必须随 leaderboard 发布。不能只说“我们没有故意泄漏”。

---

## 9. 基座选择：4B、8B、约 16B、32B 各自做什么

### 9.1 基座选择原则

首选 instruction-tuned open-weight 模型，因为第三阶段主要学习 workflow 行为而不是从零学习语言。候选必须满足：

- 权重、训练、衍生模型和输出使用许可明确；
- 能稳定生成原生 JSON/function call；
- Python/R/命令行代码能力足够；
- 英文论文与中文交互能力；
- 8K–32K 实际训练/推理上下文稳定；不能只看声明的最大窗口；
- tokenizer 对基因符号、HGNC/Ensembl ID、公式、文件路径和包名不过度碎片化；
- 可由团队硬件以 bf16/量化方式部署；
- 推理引擎、LoRA/QLoRA、长上下文和 tool template 支持成熟；
- 基座发布日期和知识截止可记录。

Qwen3 公开了 8B、14B 和 32B 等密集规模、Apache 2.0 许可和工具/长上下文支持，可作为同家族 scaling 实验的候选；但具体版本应在训练启动时重新冻结和安全审计，而不是在方案阶段永久绑定。[^qwen3] Llama 3.1 8B、MedGemma 4B/27B 或同期其他开放模型可作为对照；“医学预训练”不能替代代码、工具和多轮 workflow 的实测。

### 9.2 先跑 50 个任务的基座 bake-off

在 Phase 1 train/dev 中抽取不进入正式 test 的代表性小集，统一 P0/P1：

| 维度 | 指标 |
|---|---|
| Tool syntax | JSON parse、工具名、必需参数、类型正确率 |
| Code | 运行成功、测试通过、包/函数幻觉率 |
| Scientific invariants | 推断单位、counts layer、设计矩阵、gene set、负对照 |
| Long-horizon | 状态一致性、重复调用、早停、恢复成功 |
| Evidence | claim grounding、引用真实性、不确定性 |
| Language | 中英文任务等价性和术语稳定性 |
| Systems | tokens/s、峰值显存、并发、量化回退 |
| License/security | 是否允许预期训练与部署方式 |

选择**总体最强且没有关键硬伤的 7–9B**，而不是只看通用排行榜。

### 9.3 各规模的角色

| 规模 | 推荐角色 | 是否正式发表 |
|---|---|---|
| 4B | 数据格式、训练脚本、loss mask 和 serving 烟雾测试；也可做极低成本边界 | 可作为效率基线，不是主模型 |
| 7–9B | 第一个正式 student；完成 SFT、DPO 与必要时 RL | 是，主线 |
| 14–16B | 仅当 8B 残余错误是长程规划/证据整合且数据学习曲线未饱和 | 条件性 |
| 30–32B | 性能上限与本地部署代价的 Pareto 对照；或在 16B 明显改善后继续 | 条件性，不默认训练 |

若同一模型家族没有精确 16B，可使用 14B/15B 等最近规模，但必须报告真实参数量，不能为了符合计划把 14B 称为 16B。

---

## 10. 训练课程：从最便宜、最可验证的阶段开始

### 10.1 Stage T0：训练管线烟雾测试

目的不是拿科学结果，而是发现：

- chat template 和 tool-call token 是否正确；
- loss 是否只作用于目标 assistant；
- tool outputs 是否被误监督；
- 长样本是否截断了当前状态或 final；
- 数据分割和去重是否真正生效；
- adapter 是否能被推理引擎加载；
- checkpoint 是否可重复评测。

用 4B 或选定 8B 的小数据 QLoRA，训练 200–500 个 episode 的派生样本。若连 schema 和基本 tool calls 都没有改善，先修数据与模板，不进入大规模 SFT。

### 10.2 Stage T1：原子 skill 与工具 SFT

先训练短、确定的状态转移：

- 数据 manifest 解析；
- accession/version/hash 检查；
- tool selection 与合法 JSON；
- counts/normalized/integrated 识别；
- sample/patient 层级与设计矩阵预检；
- gene ID/物种/背景校验；
- validator 错误读取；
- claim/evidence JSON；
- `stop/request_input/inconclusive`。

这一阶段让模型获得“语法与不变量地基”，避免 full episode 的稀疏信号被长上下文淹没。

### 10.3 Stage T2：多轮状态 SFT

加入完整或半完整 episode：

```text
observation → action → execution → validator → correction → final
```

课程顺序：

1. 单工具、无错误；
2. 多工具、确定顺序；
3. 含一个可修复错误；
4. 需要回退到早期状态；
5. 数据不足的正确停止；
6. 多条合理路径；
7. 开放假设和外部验证。

不要按文本长度自动定义难度。参考 VeriReason 的做法，可用基座对同一任务多次 rollout：全错是当前过难，全对是当前过易，成功率处于中间且错误多样的任务通常提供更强学习信号。[^verireason] 但“过难”任务不应永久删除；它们可在后续模型能力提高后重新进入 curriculum。

### 10.4 Stage T3：偏好优化

SFT 有明确 dev 增益后，再构建 DPO。DPO 不需要单独训练奖励模型或在线 rollout，通常比直接上 RL 简单，适合作为本项目第二步。[^dpo]

重点偏好对：

- 正确数据版本 > 标题相似但 accession 错误；
- patient/sample-aware 分析 > cell-level 伪重复；
- 预注册主分析 + 敏感性 > 反复调参直到显著；
- 有 matched-null/负对照 > 只有富集 p 值；
- “表达程序富集” > “代谢通量升高”；
- claim 指向工件 > 无来源流畅陈述；
- `inconclusive` > 数据不足时猜测；
- 可重放的简洁轨迹 > 同等正确但重复调用的轨迹；
- 遇到不一致先定位原因 > 隐藏冲突强行综合；
- 明确请求缺失信息 > 编造 metadata。

偏好优化不应把“更像论文语言”“更长”“更自信”当作 chosen。

### 10.5 Stage T4：多轮 tool-in-the-loop RL（条件性）

只有以下条件全部满足才启动：

1. SFT/DPO student 已能稳定解析工具反馈；
2. reward 主要来自确定性执行和已验证 rubric；
3. reward 与盲法专家排序在独立校准集上有足够一致性；
4. 对 reward hacking 做过专门 red-team；
5. 训练环境与 test 完全隔离；
6. 无 gold 的正确停止也能获得高分；
7. 预算允许多 rollout 和真实工具执行。

ReTool、VeriReason 和 ATHENA-R1 都支持“模型在 rollout 中真实调用工具并根据结果继续”的路线；静态地对一段伪工具文本做 RL 不能训练真正的环境交互。[^retool][^verireason][^athena-methods]

### 10.6 Stage T5：continued pretraining（诊断后才做）

只有错误分析显示主要瓶颈是：

- 生物学术语与基因/通路文本理解；
- 论文 Methods 语言；
- 领域知识缺口无法由运行时检索合理覆盖；

才考虑对合法、去重的领域语料做 continued pretraining。若瓶颈是工具参数、统计单位或状态丢失，CPT 不会解决问题，反而增加灾难性遗忘和成本。

CPT 后必须重新进行 instruction/trajectory alignment；并与“只改 RAG/skills”的便宜方案比较。领域微调可能损害幻觉控制和指令遵循，因此通用能力回归是硬要求。[^biomed-ft][^forgetting]

---

## 11. SFT 的具体训练配方

### 11.1 推荐的首轮方法

首轮使用 QLoRA 或 LoRA，而不是 full-parameter training。QLoRA 通过 4-bit frozen base + low-rank adapters 显著降低显存，适合快速比较数据配方；原论文甚至演示了单张 48 GB GPU 微调 65B，但实际需求强烈依赖上下文、batch、实现和激活，不能据此承诺本项目硬件。[^qlora]

推荐顺序：

```text
4-bit QLoRA feasibility
      ↓
LoRA/bf16 复核最优数据配方
      ↓
只有 adapter 明显受限且数据规模足够，才比较 full fine-tuning
```

### 11.2 起始超参数搜索范围

以下只是首轮工程搜索空间，必须在 dev 上选择并完整记录：

| 项目 | 建议起始范围 |
|---|---|
| Precision | bf16 compute；QLoRA 使用 NF4 + double quantization |
| LoRA rank | 32、64；若明显欠拟合再试 128 |
| LoRA alpha | 与 rank 同量级或 2×rank，作为小网格 |
| Target modules | attention 与 MLP linear layers；先做 all-linear 与 q/k/v/o 两个配置 |
| Dropout | 0–0.05 |
| LoRA learning rate | `5e-5`、`1e-4`、`2e-4` |
| Epoch | 1–3；以 dev 与通用回归早停，不以 train loss 最低选模 |
| Warmup | 总步数 3–5% |
| Scheduler | cosine 或 linear，二选一即可 |
| Max sequence | 先 8K；确认长轨迹确有增益后试 16K/32K |
| Packing | 对短 step 样本开启；episode 样本不得跨 leakage group 混拼语义 |
| Gradient clipping | 1.0 起始 |
| Seed | 至少 3 个训练 seed 用于主比较，资源不足时先 1 个筛选再复核 3 个 |
| Checkpoint | 按固定 token/step 保存；选择规则预注册 |

不建议一开始训练 128K 上下文。外部状态、artifact refs 和受控摘要比强行把整套矩阵/日志塞入上下文更便宜、更可审计。

### 11.3 数据混合

初始建议按**采样权重**而非机械复制数据：

- 50–65%：正确原子与多轮 workflow actions；
- 15–25%：错误反馈后的正确恢复；
- 10–15%：正确停止、缺失信息和 inconclusive；
- 5–15%：通用 code/tool/instruction 回放数据，用于降低能力遗忘；
- 少量：中英文等价任务和安全拒绝。

这是起始配方，需要做消融。通用回放数据必须许可证兼容、去污染，并且不能包含正式 test。已有灾难性遗忘研究表明，后续加入通用 instruction 数据可缓解部分能力回退。[^forgetting]

### 11.4 Loss 与长度控制

- system/user/tool observation 不计算 loss；
- 只训练目标 assistant 的 `decision_summary`、structured action/tool call 和 final claim；
- `decision_summary` 有字段和长度上限，避免把冗长文本当作能力；
- 不要求模型逐字复制工具输出；
- 工具调用 ID 等随机字段不作为预测目标，可由 runtime 生成；
- 对等价工具参数格式先 canonicalize，防止仅因空格/顺序制造损失；
- 数值结论尽量由工件读取，训练 target 保留合理精度与单位，不死记完整长表。

### 11.5 Checkpoint 选择

每个 checkpoint 同时看：

1. Dev scientific score；
2. critical hard-failure rate；
3. tool schema validity；
4. recovery 与 inconclusive；
5. 通用 code/tool 指标回退；
6. 成本与长度。

若 scientific score 上升但伪重复、数据版本错误或无证据 claim 增加，checkpoint 不合格。不能用平均分补偿 critical failure。

---

## 12. Preference/DPO 的数据与训练设计

### 12.1 Preference 来源优先级

1. 确定性 validator 直接判定；
2. 工件执行与 task-specific grader；
3. 生物统计/领域专家盲法比较；
4. 独立模型 reviewer 作为辅助；
5. 同一模型自我评价仅作候选，不作最终标签。

### 12.2 Pair 生成

对每个状态采样 3–8 个候选动作，执行能执行的候选，再按以下层级比较：

```text
安全/泄漏硬门
  > 数据与统计合法性
  > 工件/任务正确性
  > 证据与校准
  > 恢复与停止
  > 在同等正确下的成本和简洁性
```

不同层级不能简单互换。例如“非常便宜但用了错误推断单位”的动作永远不能胜过较贵但科学有效的动作。

### 12.3 DPO 起始配置

可从 5,000–20,000 个高置信 pair 开始；这个数量同样由 learning curve 调整。首轮只训练 1 个 epoch，较低学习率（如 `5e-7` 至 `5e-6`）和小范围 `beta`（如 0.05、0.1、0.2）筛选，防止过强偏好把模型推离 SFT 分布。所有配置必须对同一 SFT checkpoint、同一 pair 集和同一 dev 评价。

### 12.4 DPO 的停止条件

- chosen/rejected 训练准确率继续升高，但 dev scientific score 不升；
- 模型过度拒绝或过度保守；
- 对负结果能校准，但真实正结果 recovery 下降；
- 输出更符合模板，却减少必要的工具调用；
- 通用工具和代码能力显著回退。

---

## 13. 强化学习与科学反馈奖励

### 13.1 为什么不能奖励“像论文”

本项目的最危险奖励包括：

- 是否重新得到论文 headline；
- 显著结果数量；
- p 值越小越好；
- 工具调用越多越好；
- 推理越长越好；
- 与 LLM judge 文风越接近越好。

这些信号会鼓励答案记忆、p-hacking、重复调用和冗长合理化。已有研究发现，过程奖励可能被“重复正确但无必要的步骤”利用；因此 reward 设计必须专门防范这种 hacking。[^reward-hacking]

### 13.2 Reward 先是向量，最后才标量化

每条 rollout 先保留分项：

```yaml
reward_vector:
  safety_gate: 1
  no_gold_leak: 1
  data_identity: 1
  schema_validity: 1
  inference_unit_validity: 1
  execution_success: 1
  task_artifact_score: 0.82
  robustness_score: 0.71
  claim_grounding: 0.95
  calibration: 0.90
  recovery: 1
  unnecessary_cost: 0.08
termination: finish_supported
```

论文和 dashboard 报告每个分项；训练时才按预注册规则合成 scalar。

### 13.3 硬门与分段奖励

推荐：

```text
若 safety/leakage/privacy 失败 → rollout 无资格或强负奖励
若 data identity / inference unit 关键失败 → 总奖励封顶为低分
否则：
  R = W_artifact · task_artifact_score
    + W_method   · scientific_validity
    + W_robust   · robustness
    + W_claim    · evidence_grounding
    + W_calib    · calibrated_conclusion
    + W_repair   · valid_recovery
    - W_cost     · avoidable_cost
    - W_repeat   · redundant_actions
```

规则：

- `task_artifact_score` 是主信号；
- 格式奖励只是必要条件，不能压过科学正确性；
- 成本只在两条轨迹科学等价时排序；
- 不规定最少工具次数或固定字数；
- 正确的 null/inconclusive 可获得满分；
- 与论文方向相反但数据、方法和证据正确的结果不应被惩罚；
- open hypothesis 任务不适合在早期用单一 outcome reward 做 RL。

### 13.4 适合首轮 RL 的任务

优先：

- tool schema 与参数；
- 数据版本/哈希；
- 可执行代码与测试；
- 设计矩阵和推断单位 validator；
- 已冻结输入下的统计量/排序/集合恢复；
- claim-evidence 链完整性；
- 恢复和有条件停止。

暂缓：

- 新颖性判断；
- 机制因果推断；
- 开放式文献综合；
- 临床价值排序；
- 无可复核 gold 的“好假设”；
- 需要未来湿实验才能知道的发现。

### 13.5 RL 环境

- 与 Phase 2 同一个容器镜像、skill/tool registry 和 validators；
- 每个 rollout 独立可写目录，原始输入只读；
- 网络默认关闭，仅开放任务允许的白名单服务；
- 工具错误作为 observation 返回，单次错误不立即终止；
- 重复错误、空动作、资源超限和 turn cap 才终止；
- gold 只在 grader 进程，不进入模型上下文；
- 每个 rollout 记录事件、资源、终止原因和 reward components；
- 训练和评价工具版本分开冻结，以测试新工具泛化。

### 13.6 RL 算法策略

不预先承诺 GRPO 一定优于其他方法。若采用 GRPO，可从每题 4–8 个 rollout、冻结 SFT reference 和 KL 约束开始；ATHENA-R1 公开使用每题 5 个 rollout、on-policy tool interaction 和 KL 约束，可作为实现参考而不是本项目默认超参数。[^athena-methods]

在正式大训练前需要：

- 100–300 个 train prompt 的小规模 RL；
- 人工审查高奖励轨迹；
- 比较 reward 与专家 blind ranking；
- 构造故意作弊轨迹测试 grader；
- 验证没有通过读文件名、日志或缓存取得 gold；
- 检查 reward 提升是否伴随真实工件提升。

### 13.7 RL Go/No-Go

Go：

- reward–expert 一致性达到预注册目标；
- 作弊轨迹不能获得高分；
- SFT policy 在训练任务上既有成功也有失败，rollout 间存在可学习方差；
- 小试验提高 dev 工件分数且 critical failure 不增。

No-Go：

- reward 主要依赖 LLM judge；
- 高分来自长度、模板或固定工具顺序；
- grader 自己尚不稳定；
- 大部分任务全对或全错；
- open-ended 结论没有独立验证；
- 资源预算不足以保存和审计 rollout。

---

## 14. 针对本项目三类核心 workflow 的训练设计

### 14.1 恶性细胞转录程序再发现 `WF-3CA-MP-01`

student 需要学习的关键决策：

- 确认 3CA 冻结版本和 malignant-cell subset；
- 识别 sample/patient/cancer 层级；
- 按样本或可解释单元构建 program，而非把所有细胞一次 NMF 后当作泛癌 ITH；
- 多 K、多 seed 稳定性；
- program recurrence 按独立样本/患者计；
- known MP matching 与 novel candidate 分开；
- 对癌种组成、细胞数和批次做敏感性；
- 图由表重建，claim 指向工件。

典型恢复样本：

```text
前缀：NMF 结果随 seed 大幅改变，validator=unstable_program
错误动作：挑一个最接近论文图的 seed 继续
正确目标：增加预注册 seed，计算稳定性；若仍不稳，降低 claim 强度或报告未复现
```

可执行奖励：

- 数据哈希；
- program×gene 和 program×sample 工件；
- seed/K 稳定性；
- recurrence；
- 与 train 论文 reference program 的匹配分数；
- null/permutation；
- fresh-environment reproduction；
- claim grounding。

### 14.2 代谢相关 program `WF-3CA-MET-01`

关键决策：

- gene set 的来源、版本、物种、ID 映射与未映射比例；
- 背景 universe；
- ORA 与 rank-based enrichment 的适用条件；
- gene-set 大小、表达水平等 matched null；
- 患者级活性和跨癌种异质性；
- external validation；
- 表达、通路活性推断和实测 flux 的语言边界。

高价值 preference：

```text
chosen: “代谢基因集在 MP-X 中呈排名富集；结果对 matched-null 稳健，但仅支持转录程序关联。”
rejected: “MP-X 的代谢通量显著升高，说明该通路驱动肿瘤进展。”
```

### 14.3 T-cell/TCR 与 BKI 扩展

公开数据阶段学习：

- clonotype → cell state → sample/patient → study 层级；
- clone expansion 与细胞数量的混淆；
- 患者/队列分割，禁止同一患者跨 train/test；
- TCR/表达配对质量；
- clone-aware uncertainty；
- 肿瘤反应性 score 的外部验证和反例。

BKI 数据阶段：

- 默认只在隔离环境训练私有 adapter；
- 公共 checkpoint 不接触患者原始数据、衍生可识别特征、未公开结论或轨迹；
- 数据删除时，内容寻址 lineage 能定位受影响 adapter；必要时从干净 release 重训，而不是承诺不可靠的精确 unlearning；
- 任何跨机构共享需要 DUA、伦理和合作协议明确允许模型训练与参数发布。

---

## 15. 评价设计：训练是否真的有用

### 15.1 主对照矩阵

使用相同输入、预算、tool/skill 版本和 grader：

| ID | 模型 | Workflow | 用途 |
|---|---|---|---|
| E0 | Base 8B | Direct | 小模型零训练基线 |
| E1 | Base 8B | Full Flow | skills/workflow 对小模型的增益 |
| E2 | SFT 8B | Direct | 训练本身是否编码了部分流程 |
| E3 | SFT 8B | Full Flow | 主结果：训练与 workflow 是否互补 |
| E4 | SFT+DPO 8B | Full Flow | 偏好优化增量 |
| E5 | SFT+DPO+RL 8B | Full Flow | 条件性 RL 增量 |
| E6 | Frontier model | Direct | 模型规模基线 |
| E7 | Frontier model | Full Flow | 当前性能上限 |
| E8 | 约 16B/32B student | Full Flow | 条件性 scaling/Pareto |

不能只比较 `SFT 8B + Flow` 与 `Frontier Direct`，否则无法知道提升来自训练还是 workflow。

### 15.2 训练数据消融

| Ablation | 回答的问题 |
|---|---|
| Final answer only | 只记问答是否足够 |
| Plan + final | 分析计划是否贡献 |
| Observable actions + outputs | 执行轨迹是否贡献 |
| + validator feedback/recovery | 恢复样本是否贡献 |
| + correct stop/inconclusive | 是否改善校准 |
| Synthetic only vs real verified | 合成轨迹能否泛化 |
| Gold-assisted vs gold-blind | Helper 是否造成答案捷径 |
| No skills at inference | student 是否过度依赖或真正内化 |
| No artifact retrieval | 长任务是否依赖外部状态 |

### 15.3 评价维度

#### 科学正确性

- task-specific artifact score；
- program/gene-set recovery、rank correlation、方向；
- 效应量、CI、校准；
- sample/patient-aware inference；
- 外部队列复现；
- null/inconclusive 正确率；
- conclusion 与 evidence 一致性。

#### 工作流可靠性

- data identity pass rate；
- tool schema 和执行成功率；
- invalid/redundant call rate；
- recovery success；
- hard-gate violation；
- clean-environment replay；
- artifact completeness；
- claim-evidence coverage。

#### 泛化

- 新论文族；
- 新数据 accession/版本；
- 新癌种或队列；
- 新列名/工具描述；
- 未见工具但有完整 schema；
- 时间滚动新任务；
- conclusion-flipped/null 数据。

#### 成本与部署

- 每个成功任务的 GPU/API/CPU 成本；
- 端到端延迟与工具等待；
- 输入、输出和 reasoning token；
- 峰值显存、模型磁盘和吞吐；
- 离线运行比例；
- 人工审核分钟数。

#### 通用能力回归

- 通用代码与函数调用；
- 中英文指令遵循；
- 长上下文状态；
- 引用幻觉；
- 安全拒绝；
- 未见任务上的过度拒绝。

### 15.4 统计报告

- 任务是主要分析单位，不把同一任务的多个 turn 当独立样本；
- 对 task family/paper family 做分层 bootstrap 或层级模型；
- 报告绝对差、相对差和 95% CI；
- 主模型至少 3 个训练 seed，推理有随机性时每任务多次运行；
- 预注册主指标和 critical failures；
- 多任务比较进行适当校正；
- 同时报告 coverage 与 conditional accuracy，防止靠全部拒绝获得安全高分；
- leaderboard 保留每个任务族和 Pareto，不用单一平均分掩盖短板。

### 15.5 成功判据

“小模型达到或超过前沿模型”只能写成研究目标。建议分层定义：

- **最低成功**：`SFT 8B + Flow` 在 dev/sealed scientific score 上稳定优于 `Base 8B + Flow`，critical failure 不增加；
- **方法成功**：完整轨迹显著优于 final-answer-only，且 validator recovery 消融有增量；
- **系统成功**：在至少一个核心任务族，8B + Flow 落在 Frontier + Flow 的预设等效界值内，同时成本/隐私更优；
- **部署成功**：受控环境可离线运行、审计、重放，并满足 BKI 权限要求；
- **扩展成功**：约 16B/32B 的提升符合残余错误假设，而不是只提高文风或 benchmark 记忆。

前文提出的“达到最佳通用系统 90%、可变成本不超过 25%”可以保留为 aspirational target，但不应在观察数据后才用它定义成功。

---

## 16. 何时从 8B 扩到约 16B 和 32B

### 16.1 错误驱动决策表

| 8B 的主要残余错误 | 优先修复 | 扩模型？ |
|---|---|---|
| JSON/tool 参数错误 | schema、grammar、短 SFT 样本 | 否 |
| accession/版本错误 | dataset resolver 与 hard gate | 否 |
| cell 当患者 | hard negatives、preference、validator | 否 |
| 代码 API 幻觉 | 文档检索、执行反馈、修复轨迹 | 通常否 |
| 长历史丢失 | 外部 state、artifact retrieval、摘要 | 不一定 |
| 领域知识过时 | live retrieval；必要时 CPT | 不一定 |
| 开放假设缺少多证据整合 | 增加高质量跨证据任务；再比较约 16B | 是，条件性 |
| 8B 在充分数据下 learning curve 仍上升且明显欠拟合 | 约 16B 对照 | 是 |
| 约 16B 有稳定收益但未饱和，且本地部署允许 | 32B 上限 | 条件性 |
| 8B 已满足目标或收益被 workflow 消除 | 保持 8B | 否 |

### 16.2 公平 scaling 实验

- 尽量使用同一模型家族的 8B/14–16B/30–32B；
- 相同训练样本、token budget、数据顺序原则和 evaluator；
- 分别报告 LoRA rank、训练 FLOPs/GPU-hours 和推理预算；
- 不让大模型获得更长工具预算后再归因于参数规模；
- 画 scientific score–cost–latency–memory Pareto；
- 分开短任务与长任务。

RF-Agent 报告小中型模型从领域 SFT 获益较大且更大模型收益可能递减；这支持做 learning curve，但不预先保证 8B 会胜过 32B。[^rfagent]

---

## 17. 训练资源与工程实现

### 17.1 资源估算原则

不要仅按参数量估显存。至少记录：

- 权重精度与量化；
- optimizer state；
- LoRA rank/target modules；
- sequence length；
- micro-batch 与 gradient accumulation；
- gradient checkpointing；
- attention 实现；
- rollout 数与并发工具；
- 工件读取和容器开销。

ATHENA-R1 的 8B LoRA SFT 使用 4×80 GB H100 和约 9.93 GPU-days，是一个公开的长轨迹工程参照，不是本项目最低门槛。[^athena-methods] QLoRA 可以显著降低权重与优化显存，但长序列激活和多轮 RL rollout 仍可能成为主成本。

### 17.2 推荐资源阶段

| 阶段 | 最简实现 | 扩展条件 |
|---|---|---|
| Smoke | 单机 QLoRA、短上下文、小数据 | schema 正确后停止，不追求分数 |
| 8B SFT | 1–4 张现代 GPU，取决于上下文和吞吐 | 由实际 profiling 定 batch |
| DPO | 复用 SFT 训练栈；小 pair 集先筛选 | dev 有稳定增益才扩大 |
| RL | 独立 rollout workers + tool sandbox + trainer | 先证明 reward 可靠；通常最昂贵 |
| 16/32B | 多 GPU 或更强量化/并行 | 8B error analysis 明确支持 |

### 17.3 可复现的 `training_run.yaml`

```yaml
training_run_id: tr_2027_001
parent_checkpoint: qwen3-8b-instruct@sha256:...
model_license_snapshot: licenses/model.txt
dataset_release: biodiscovery-trajectories-v0.1.0
dataset_hash: sha256:...
split_hash: sha256:...
method: qlora_sft
chat_template_hash: sha256:...
tool_schema_version: v0.1.0
skillset_version: v0.1.0
environment_image: sha256:...
hyperparameters:
  seed: 17
  max_sequence_length: 8192
  learning_rate: 0.0001
  epochs: 2
  lora_rank: 64
  lora_alpha: 128
  lora_dropout: 0.05
  quantization: nf4_double_quant
loss_mask: assistant_targets_only
hardware:
  gpu_type: "record_exactly"
  gpu_count: 1
software_lock: environment.lock
selection_rule: max_dev_scientific_subject_to_zero_critical_regression
sealed_test_access: false
```

### 17.4 每次 checkpoint 必须附带

- base model 与 license；
- dataset release/hash 和过滤规则；
- 训练方法、总 tokens、GPU-hours、排放/能耗如可得；
- 适用任务与禁止用途；
- Phase 1 dev/sealed/OOD 结果；
- critical failure 与已知退化；
- 隐私和数据来源；
- quantization 后的独立评测；
- 可重现 serving 配置；
- 删除/撤回某数据时受影响 checkpoint 的 lineage。

---

## 18. 隐私、许可和安全治理

### 18.1 每个样本的权利字段

```yaml
rights:
  source_data_license: "..."
  paper_text_license: "..."
  code_license: "..."
  teacher_output_terms_snapshot: "..."
  model_derivative_license: "..."
  training_allowed: true
  redistribution_allowed: false
  commercial_use_allowed: false
  expiration_or_revocation: null
  reviewer: "..."
```

数据“可公开下载”不自动等于“可重新分发、可训练、可商用”。论文全文、补充材料、论坛讨论、代码、患者数据和教师模型输出需要分别核对。

### 18.2 公开/受控双轨

| Release | 可含内容 | 部署 |
|---|---|---|
| Public student | 公开/明确授权数据、合成任务、公开 skills | 可发布权重或 adapter，依许可证 |
| Restricted research student | 合作方允许训练但不允许公开的数据 | 受控访问，不发布原始轨迹 |
| BKI private adapter | 明确授权的内部/患者相关轨迹 | 隔离环境、无默认公网、访问审计 |

推荐尽量保留公共 base + 可替换 adapter。若某受控数据需要删除，从干净的公共 checkpoint 重训私有 adapter通常比尝试证明精确 unlearning 更可审计。

### 18.3 日志脱敏

构建数据时扫描并删除：

- API keys、tokens、cookies、SSH 信息；
- 用户名和个人绝对路径；
- 内部主机、数据库连接串、未公开仓库；
- 直接患者标识；
- 日期、稀有组合、自由文本等可能再识别字段；
- 审稿中论文和未公开结果；
- reviewer 的个人信息。

“去掉姓名”不足以完成患者数据匿名化；应由机构隐私/伦理流程判定。

### 18.4 安全测试

- prompt injection 试图读取 sealed gold；
- 工具输出中夹带恶意指令；
- 路径穿越、读取凭据和越权网络；
- 将患者数据发送给外部 API；
- 伪造 PMID/DOI/工件；
- 通过文件名或缓存猜答案；
- 故意使用细胞数制造极小 p 值；
- 自动生成临床建议或双重用途实验步骤超出范围。

---

## 19. 项目实施路线图

### M0：准备与冻结（4–6 周）

- 冻结 Phase 1 split/leakage groups；
- 冻结 Phase 2 core8 skills 与事件 schema；
- 建训练权利表和 privacy tiers；
- 完成 Base 8B P0/P1；
- 定义主指标、critical failure 和 checkpoint 选择规则。

交付：`Training Readiness Report`。任何 G0–G7 未通过则 No-Go。

### M1：轨迹数据工厂与 smoke（6–8 周）

- 从 200–500 个 episode 生成四种派生视图；
- 完成 schema、去重、泄漏、隐私、许可审计；
- 训练 4B 或小规模 8B QLoRA；
- 检查 loss mask、tool calls、重放和 serving。

交付：`trajectory-dataset-v0.0.1`、smoke checkpoint 和错误清单。

### M2：首个 8B SFT（8–12 周）

- 扩到至少约 2,000 个合格 episode；
- 训练 T1 原子 skill，再训练 T2 多轮状态；
- 做三 seed 与 10/25/50/100% learning curve；
- 运行 E0–E3 和数据消融；
- 专家盲审 claims 与关键失败。

交付：`BioDiscovery-Workflow-Student-8B-SFT-v0.1`、dataset card、model card、benchmark 报告。

### M3：偏好优化（6–10 周）

- 从 validator、执行候选和专家裁决产生 5k–20k pairs；
- DPO 小网格；
- 重点测 pseudoreplication、因果升级、证据缺失、inconclusive 和成本；
- 运行 E3–E4。

交付：8B-DPO checkpoint 与 pair 数据说明。

### M4：RL 可行性审计（4–8 周）

- 只选可执行、可判分的 train task；
- 校准 reward–expert 一致性；
- 构造 reward hacking 测试；
- 100–300 prompt 小规模多轮 rollout；
- 通过 Go/No-Go 后才扩大。

交付：RL audit。若 No-Go，保留 SFT/DPO 作为正式结果，不把未做 RL 视为失败。

### M5：规模、外部验证与部署（8–16 周）

- 根据 8B 错误选择是否训练约 16B；
- 只有 16B 有清晰增益才考虑 32B；
- rolling temporal/OOD；
- 公开数据模型发布和 BKI 私有 adapter 审计；
- 形成性能–成本–隐私 Pareto 和 leaderboard。

---

## 20. 团队角色与审核责任

| 角色 | 主要责任 | 不得单独决定 |
|---|---|---|
| Benchmark curator | split、gold、leakage group、任务有效性 | 训练 checkpoint 选择 |
| Workflow engineer | events、skills、tools、replay、环境 | 科学 gold |
| Bioinformatician | 分析方法、工件和错误类型 | 患者数据许可 |
| Biostatistician | 推断单位、模型式、CI、敏感性 | 工具/平台安全 |
| Domain biologist | 生物解释、反例、实验可检验性 | 统计有效性 |
| ML engineer | 数据派生、训练、serving、profiling | 修改 sealed grader |
| Data steward/privacy | DUA、伦理、脱敏、发布等级 | 科学结论 |
| Independent reviewer | 盲法审查和争议裁决 | 访问 train prompt 优化记录 |

高风险轨迹至少需要统计和领域两类审核；普通 Tier B 可用自动 validator + 分层抽样人审。主动学习优先把 reviewer 时间用于：模型间分歧、validator 冲突、临界分数、长链和高影响 claim。

---

## 21. 论文与项目的核心创新点

第三阶段论文不应把创新写成“我们微调了一个生物医学 8B”。更有价值的贡献是：

1. **Workflow-native trajectory dataset**：轨迹从真实数据、工具、统计和工件产生，而不是只有合成问答；
2. **Observable, executable supervision**：状态、动作、工具反馈、validator、修复和 claims 可重放，不依赖 hidden CoT；
3. **Scientific failure supervision**：伪重复、数据版本、混杂、负对照、代谢过度解释和正确拒绝被显式编码；
4. **Gold-blind rediscovery**：student 必须从数据发现，而不是读取论文结论；
5. **Skills × training 因果消融**：区分模型参数、workflow 和二者交互；
6. **SFT → DPO → 条件性 RL 的证据门控路线**；
7. **成本、隐私和部署 Pareto**：回答小模型的实际动机，而不只报平均 accuracy；
8. **可删除、可审计的公共/私有 adapter 双轨**。

---

## 22. 最终推荐：首个版本只做什么

### 必做

1. 冻结 Phase 1 train/dev/sealed 和 leakage groups；
2. 用 Phase 2 core8 workflow 采集至少约 2,000 个合格 episode；
3. 保存可观察轨迹，不依赖私有 CoT；
4. 建四个派生文件：`sft_step`、`sft_episode`、`preference`、`rl_prompts`；
5. 先做一个 8B QLoRA/LoRA SFT；
6. 完成 E0–E3 与 final-only/full-trajectory/recovery 消融；
7. 把科学 hard failures、成本、隐私和通用回退与总分同等报告；
8. 只有 SFT 通过后做 DPO；只有 reward 审计通过后做 RL；
9. 由 8B 的残余错误决定约 16B/32B，而不是按路线图惯性扩模。

### 暂不做

- 同时训练 planner、executor、reviewer 三个小模型；
- 从一开始 full-parameter 训练 32B；
- 将所有论文全文做 continued pretraining；
- 用长 CoT 作为唯一质量标签；
- 对开放科学假设直接做 outcome RL；
- 把 BKI/患者轨迹默认加入公共训练；
- 取消 Phase 2 skills/validators，试图让模型权重包办全部流程。

### 第一项可证伪的主实验

```text
同一个 8B base、同一个 Phase 2 workflow、同一批 sealed tasks：

Base + Flow
vs.
Final-answer SFT + Flow
vs.
Observable-trajectory SFT + Flow
vs.
Observable-trajectory + recovery/inconclusive SFT + Flow
```

若完整轨迹组不能稳定优于 final-answer 组，说明当前轨迹字段、质量过滤或任务覆盖没有提供额外学习信号；此时不应继续扩大模型或做 RL。这个负结果同样重要，因为它直接检验第三阶段最核心的研究假设。

---

## 23. 参考文献与在线资源

### 23.1 检索方法与冲突记录

本轮检索围绕四组概念展开：`agent trajectory distillation / tool-use SFT`、`multi-turn execution-feedback RL`、`biomedical agent post-training`、`chip-design domain model training`。来源优先级为：期刊/会议正式页面和论文 Online Methods；其次是 arXiv/bioRxiv 原文；官方项目仓库只用于核对代码、模型或数据是否公开，不用第三方摘要替代论文结论。重复版本按 DOI、arXiv ID 和标题归并，正式版优先于预印本；2026 年新工作明确标注其预印本或单次实验性质。

需要保留的证据冲突与不确定性：

- RF-Agent 的正式实验规模是 0.6B–4B，而不是 7B/8B；它支持教材 QTSA 蒸馏，但不能作为“真实 EDA workflow 轨迹已训练 8B”的证据。
- PDAgent-Bench 和 HeaRT 产生或展示有价值的工作流/推理记录，但公开论文没有把这些真实轨迹用于 student SFT；本方案只借鉴其状态、工具闭环和层级表示。
- VeriReason 有强执行反馈，但 Verilog testbench 比开放生物医学发现更接近确定性真值；其 reward 不能直接复制。
- ATHENA-R1 是目前最直接的生物医学 SFT + 多轮工具 RL 证据，但任务主要是药物/治疗选择，其答案映射和固定结构奖励不适合直接迁移到组学再发现。
- Biomni 官方仓库称 Biomni-R0-32B 使用 agent interaction RL，但检索时所链接的 technical report 页面不可用；因此本方案没有用该预览声明推导数据量、奖励或训练参数。
- BixBench-Verified-50 的近饱和结果来自 2026 年单作者预印本、50 题且每配置只运行一次；它只作为“skills 与 benchmark 清理很重要”的新证据，不作为模型能力上限。
- 不同论文对 SFT、多轮交互和 RL 的结论并不一致；MINT 观察到常规 SIFT/RLHF 在其设置中可能伤害多轮能力，而 FireAct、VeriReason、ToolACE 和 ATHENA 在各自任务中报告增益。因此本项目必须保留 `Base+Flow` 与 `SFT+Flow` 的受控比较和 No-Go 机制。

### 芯片与 agent 轨迹训练

[^rfagent]: Xing Y. et al. *RF-Agent: A Practical Framework for Building Language Agents for RFIC Design*. ICLAD 2026 / arXiv:2607.18772. https://arxiv.org/abs/2607.18772

[^pdagent]: Li Q. et al. *PDAGENT-BENCH: Characterizing, Grounding, and Architecting LLM Agents for VLSI Physical Design*. arXiv:2606.17253 (2026). https://arxiv.org/abs/2606.17253

[^heart]: Poddar S. et al. *HeaRT: A Hierarchical Circuit Reasoning Tree-Based Agentic Framework for AMS Design Optimization*. arXiv:2511.19669 (2025). https://arxiv.org/abs/2511.19669

[^analogseeker]: Chen Z. et al. *AnalogSeeker: An Open-source Foundation Language Model for Analog Circuit Design*. arXiv:2508.10409 (2025). https://arxiv.org/abs/2508.10409

[^verireason]: Wang Y. et al. *VeriReason: Reinforcement Learning with Testbench Feedback for Reasoning-Enhanced Verilog Generation*. arXiv:2505.11849 (2025). https://arxiv.org/abs/2505.11849

### 生物医学 agent、benchmark 与领域微调

[^athena-methods]: Gao S. et al. *An AI agent for treatment reasoning over a biomedical tool universe* and Online Methods (2026). Paper: https://arxiv.org/abs/2606.28692 ; Online Methods: https://athena.openscientist.ai/pdf/ATHENA-R1_Online_Methods.pdf

[^crisprgpt]: Qu Y. et al. *CRISPR-GPT for agentic automation of gene-editing experiments*. **Nature Biomedical Engineering** (published online 2025; volume issue 2026). https://www.nature.com/articles/s41551-025-01463-z

[^biodsa]: Wang Z., Danek B., Sun J. *BioDSA-1K: Benchmarking Data Science Agents for Biomedical Research*. arXiv:2505.16100 (2025). https://arxiv.org/abs/2505.16100

[^skill-bix]: Zhang X. *Skill-Augmented Frontier Agents Nearly Saturate BixBench-Verified-50*. bioRxiv preprint (2026). https://www.biorxiv.org/content/10.64898/2026.04.28.721523v1

[^biomed-ft]: Jeong M. et al. *Does Biomedical Training Lead to Better Medical Performance?* arXiv:2404.04067. https://arxiv.org/abs/2404.04067

### 通用 agent/post-training 方法

[^fireact]: Chen B. et al. *FireAct: Toward Language Agent Fine-tuning*. arXiv:2310.05915. https://arxiv.org/abs/2310.05915

[^toolace]: Liu W. et al. *ToolACE: Winning the Points of LLM Function Calling*. ICLR 2025. https://proceedings.iclr.cc/paper_files/paper/2025/hash/663865ea167425c6c562cb0b6bcf76c7-Abstract-Conference.html

[^mint]: Wang Z. et al. *MINT: Evaluating LLMs in Multi-turn Interaction with Tools and Language Feedback*. ICLR 2024. https://proceedings.iclr.cc/paper_files/paper/2024/hash/8a0d3ae989a382ce6e50312bc35bf7e1-Abstract-Conference.html

[^retool]: Feng J. et al. *ReTool: Reinforcement Learning for Strategic Tool Use in LLMs*. ICLR 2026. https://proceedings.iclr.cc/paper_files/paper/2026/hash/4038c9208dfc22644c60ad39c24e5c53-Abstract-Conference.html

[^qlora]: Dettmers T. et al. *QLoRA: Efficient Finetuning of Quantized LLMs*. NeurIPS 2023. https://proceedings.neurips.cc/paper_files/paper/2023/hash/1feb87871436031bdc0f2beaa62a049b-Abstract.html

[^dpo]: Rafailov R. et al. *Direct Preference Optimization: Your Language Model is Secretly a Reward Model*. NeurIPS 2023. https://proceedings.neurips.cc/paper_files/paper/2023/hash/a85b405ed65c6477a4fe8302b5e06ce7-Abstract-Conference.html

[^forgetting]: Luo Y. et al. *An Empirical Study of Catastrophic Forgetting in Large Language Models During Continual Fine-tuning*. arXiv:2308.08747. https://arxiv.org/abs/2308.08747

[^reward-hacking]: Gao J. et al. *On Designing Effective RL Reward at Training Time for LLM Reasoning*. arXiv:2410.15115. https://arxiv.org/abs/2410.15115

[^cot-faithfulness]: Turpin M. et al. *Language Models Don't Always Say What They Think: Unfaithful Explanations in Chain-of-Thought Prompting*. arXiv:2305.04388. https://arxiv.org/abs/2305.04388 ; Chen Y. et al. *Reasoning Models Don’t Always Say What They Think* (2025). https://www.anthropic.com/research/reasoning-models-dont-say-think

[^qwen3]: Qwen Team. *Qwen3 Technical Report*. arXiv:2505.09388; official model documentation. https://arxiv.org/abs/2505.09388 ; https://github.com/QwenLM/Qwen3

### 本项目本地材料

- [会议记录稿件](./material/会议记录稿件.md)
- [三阶段完整方案](./3CA_生物医学科学发现Agent三阶段完整方案.md)
- [第一阶段 benchmark 数据集构建规则](./第一阶段_生物医学科学发现Benchmark数据集构建规则.md)
- [第二阶段可靠 agentic workflow 方案](./第二阶段_可靠生物医学AgenticWorkflow完整设计方案.md)
- 用户提供的芯片训练经验附件：`C:/Users/User/.codex/attachments/ef2602f6-875e-4301-a402-cefb274a0439/pasted-text.txt`

---

## 24. 一句话结论

第三阶段应训练“会遵守并操作可靠科学工作流的 8B 策略模型”，而不是训练一个记住论文答案、输出更长思维链的生物医学聊天模型；成功与否只能由封闭 benchmark 中的可执行科学工件、关键错误率、泛化、成本和隐私共同决定。
